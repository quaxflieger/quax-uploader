[project]
name = "quax-uploader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.136.0",
    "jinja2>=3.1.6",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
]
